import React, { Component, PropTypes } from 'react';

export default class Fix extends Component {
    constructor(props) {
        super();
    }

    render() {
        return (
            <div>
                Fix
            </div>
        );
    }
}
